# Build artifacts / source assets — kept on disk for reference, not deployed.
*.usdc
*_backup.glb
*_backup.usdz

# Vite / Node
node_modules/
dist/
*.local
.vite/

# Dev probe screenshots (throwaway)
dev/_*.png

# Dev probe output dirs (throwaway) — matches underscore-prefixed DIRS only,
# so tracked gate probes dev/_m*check.mjs / _clipcheck.mjs / _strip.mjs etc. stay safe.
dev/_*/
